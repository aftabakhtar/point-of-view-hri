# Analysis and study-configuration tooling.
# Install into a clean virtual environment:
#   python -m venv .venv && .venv/Scripts/activate   (Windows)
#   python -m venv .venv && source .venv/bin/activate (Linux/macOS)
#   pip install -r requirements.txt
#
# generate_participant_configs.py needs nothing beyond the standard library;
# the pins below are for the analysis scripts.

pandas>=2.0,<3
numpy>=1.24,<3
matplotlib>=3.7,<4

# plot_trajectory.py only -- builds the interactive robot/pedestrian animation.
plotly>=5.18,<6

# power_analysis.py only -- FTestAnovaPower for the a-priori power curves.
statsmodels>=0.14,<1
